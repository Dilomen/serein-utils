# dataURLToFile

dataUrl 转化为 Blob 数据

| 传参                  | 结果           |
| --------------------- | -------------- |
| (**dataUrl**: String) | **file**: File |

## 使用

```js
import { dataURLToFile } from 'serein-utils';
const file = dataURLToFile(dataUrl);
```
