# dataURLToBlob

dataUrl 转化为 Blob 数据

| 传参                  | 结果           |
| --------------------- | -------------- |
| (**dataUrl**: String) | **blob**: Blob |

## 使用

```js
import { dataURLToBlob } from 'serein-utils';
const blob = dataURLToBlob(dataUrl);
```
